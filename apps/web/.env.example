# Chave do Google Maps (APIs: Maps JavaScript + Geocoding).
# Exposta ao navegador — proteja com restrição de domínio HTTP no Google Cloud Console.
# Vai no Vercel (Settings → Environment Variables), NÃO no Railway.
VITE_GOOGLE_MAPS_KEY=
